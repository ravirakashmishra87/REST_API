https://rest-apis-flask.teclado.com/

1. Set veritual environment : .\.venv\Scripts\Activate.ps1
2. Build docker image Command: docker build -t <image name> .
    docker build -t flask-smorest-api .

3. Run docker container for image image
    docker run -dp 5005:5000 -w /app -v "$(pwd):/app" flask-smorest-api